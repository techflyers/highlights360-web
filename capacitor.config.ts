import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.techflyers.highlights',
  appName: 'Highlights 360',
  webDir: 'build'
};

export default config;
